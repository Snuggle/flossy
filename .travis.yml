language: python
python:
  - "3.6"
  - "3.7"
  - "3.7-dev"
  - "nightly"
services:
  - mysql
env:
  - DJANGO_VERSION=1.11.12 DB=mysql
  - DJANGO_VERSION=2.0 DB=mysql
install:
  - pip install -q Django==$DJANGO_VERSION
  - pip install -r requirements.txt
before_script:
  - mysql -e 'create database test;' -u root
script:
  - python manage.py test
matrix:
  fast_finish: true
  allow_failures:
    - python: 3.7-dev
    - python: nightly
